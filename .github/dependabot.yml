version: 2
updates:
  # Keeps the SHA-pinned actions in the workflows current, including the
  # version comments next to each pin.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
